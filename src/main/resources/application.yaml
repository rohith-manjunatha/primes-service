spring:
  datasource:
    url: jdbc:postgresql://localhost:4003/primes
    username: postgres
    password: password

  jps:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port: 8085